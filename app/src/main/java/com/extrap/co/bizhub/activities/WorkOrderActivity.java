package com.extrap.co.bizhub.activities;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;
import com.extrap.co.bizhub.R;

public class WorkOrderActivity extends AppCompatActivity {
    
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_work_order_details);
        
        // This is a placeholder activity that redirects to WorkOrderDetailsActivity
        // In a real app, this would be the main work order management activity
    }
} 